services:
  - type: static
    name: onecart-frontend
    env: static
    plan: free
    staticPublishPath: build
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
